#include <iostream>
#include <random>
#include <time.h>

// L'inclusione di ulteriori librerie è permesso,
// sebbene non strettamente necessario.

using namespace std;

int main()
{
    srand(time(NULL)); // inizializzazione della funzione rand()
    
    // INSERIRE QUI IL CODICE
    
    return 0;
}
